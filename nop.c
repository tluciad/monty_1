#include "monty.h"
/**
 * nop - doesn’t do anything
 *
 * @s: name of the stack
 * @new_value: new item to push
 * Return: Always 0
 */
void nop(stack_t **s, unsigned int new_value)
{
	(void)s;
	(void)new_value;
}
